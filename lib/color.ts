/** Standardized neutral frame background for every product image tile, so all product photography reads consistently regardless of each photo's own background or the product's brand color. */
export const NEUTRAL_FRAME_BG = "#F0F2EB";
